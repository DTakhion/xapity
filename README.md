# xapity

Agenda inteligente para peluquería (MVP con agentes / automatización).

## Setup (local)
```bash
git clone https://github.com/DTakhion/xapity.git
cd xapity
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

